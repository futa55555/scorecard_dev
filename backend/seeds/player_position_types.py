# backend/seeds/player_position_types.py

from backend import models

def seed_player_position_types(db):
    player_position_types = [
        
    ]
    db.add_all(player_position_types)
    db.commit()
